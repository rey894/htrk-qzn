export const APP_CONFIG = {
  maintenanceMode: false, // 🔒 toggle this
  allowAdminDuringMaintenance: true,
  /** Emails that always get admin access (client-side fallback if DB role not yet set) */
  adminEmailWhitelist: ['pio.quezonbukidnon@gmail.com'],
};
